package Subsequences;

public class Basic {
    /*
    A subsequence of a string is a new string formed from the original string by
    deleting some or no characters without changing the order of remaining
    characters.

    Example -
    "ace" is a subsequence of "abcde"
    "aec" is not a subsequence of "abcde"

    boolean isSubseq(String str, String seq){
       int i = 0;
       int j = 0;
       while(i<str.length() && j<seq.length()){
        if(str.charAt(i)== seq.charAt(j)){
          j++;
        }
        i++;
        }
      return j == seq.length();
      }


     */
}
