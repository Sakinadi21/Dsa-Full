package Doublyll;

public class Basic {
    /*
    * It is called two-way linked list
    * Given a node,  we can navigate list in both forward and backward direction ,
    which is not possible in Singly Linked List.
    * A node in Singly Linked List can only be deleted if we have a pointer to its previous node.
    But in doubly linked list we can delete the node even if we don't have pointer to its
    * previous node
    * ListNode in Doubly Linked List -
    <----- | previous | data | next | ----->

       public class ListNode{
       int data;
       ListNode previous;
       ListNode next;

       public ListNode(int data){
       this.data = data;
       }
   }
     */
}
