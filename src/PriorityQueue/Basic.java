package PriorityQueue;

public class Basic {
    /*
    Priority Queue is a data structure that allows us to find minimum/maximum element among a
    collection of elements in constant time . It supports following operations -

    . insert(key) - Insert a key into the priority queue
    . deleteMax() / deleteMin() - return and remove largest / smallest key.
    . getMax() / getMin() - return largest / smallest key.
     */
}
